CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

AUX_SOURCE_DIRECTORY( . SOURCES )

ADD_EXECUTABLE( DNRemoteServer ${SOURCES} )
TARGET_LINK_LIBRARIES(DNRemoteServer V2StyxServerLibrary)
TARGET_LINK_LIBRARIES(DNRemoteServer Ws2_32)